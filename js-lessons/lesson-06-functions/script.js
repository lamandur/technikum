// ==================================================
// УРОК 6: Функции
// ==================================================

/*
ТЕОРИЯ

ЧТО ТАКОЕ ФУНКЦИЯ?

Функция — это блок кода, который можно вызывать многократно.

СИНТАКСИС ОБЪЯВЛЕНИЯ ФУНКЦИИ:

function имяФункции(параметры) {
    // тело функции
    return результат;  // необязательно
}

ПРИМЕРЫ:

// Функция без параметров и без возврата
function sayHello() {
    console.log("Привет!");
}

// Функция с параметрами
function greet(name) {
    console.log("Привет, " + name + "!");
}

// Функция с возвратом значения
function add(a, b) {
    return a + b;
}

ВЫЗОВ ФУНКЦИИ:

sayHello();              // "Привет!"
greet("Иван");           // "Привет, Иван!"
let result = add(5, 3);  // result = 8

ПАРАМЕТРЫ И АРГУМЕНТЫ:

Параметры — это переменные в объявлении функции.
Аргументы — это реальные значения при вызове функции.

function multiply(x, y) {  // x, y — параметры
    return x * y;
}

multiply(4, 5);  // 4, 5 — аргументы

ВОЗВРАТ ЗНАЧЕНИЯ:

Оператор return завершает выполнение функции и возвращает значение.

function square(num) {
    return num * num;
}

let result = square(5);  // result = 25

Если функция ничего не возвращает явно, она возвращает undefined.

ОБЛАСТЬ ВИДИМОСТИ:

Переменные, объявленные внутри функции, доступны только внутри неё.

function example() {
    let x = 10;  // локальная переменная
    console.log(x);  // 10
}

example();
console.log(x);  // ошибка: x не определён

СТРЕЛОЧНЫЕ ФУНКЦИИ:

Сокращённый синтаксис для создания функций.

// Обычная функция:
function add(a, b) {
    return a + b;
}

// Стрелочная функция:
let add = (a, b) => a + b;

// С одним параметром можно опустить скобки:
let square = x => x * x;

// Без параметров нужны пустые скобки:
let sayHi = () => console.log("Привет!");

// Если нужно несколько строк, используйте фигурные скобки и return:
let multiply = (a, b) => {
    let result = a * b;
    return result;
};
*/

console.log("УРОК 6: Функции");
console.log("");

// ==================================================
// ЗАДАНИЕ 1: Создание простых функций
// ==================================================

console.log("ЗАДАНИЕ 1: Создание простых функций");
console.log("-------------------------------------");

// 1. Создайте функцию sayGoodbye(), которая выводит "До свидания!"
// 2. Создайте функцию printCurrentTime(), которая выводит текущее время
// 3. Вызовите обе функции

// Ваш код:
// Пример:
// function sayHello() {
//     console.log("Привет!");
// }
// sayHello();




console.log("");
console.log("================================");
console.log("");

// ==================================================
// ЗАДАНИЕ 2: Функции с параметрами
// ==================================================

console.log("ЗАДАНИЕ 2: Функции с параметрами");
console.log("----------------------------------");

// 1. Создайте функцию greet(name), которая выводит "Привет, [имя]!"
// 2. Создайте функцию multiply(a, b), которая выводит произведение двух чисел
// 3. Вызовите функции с разными аргументами

// Ваш код:
// Пример:
// function greet(name) {
//     console.log("Привет, " + name + "!");
// }
// greet("Иван");




console.log("");
console.log("================================");
console.log("");

// ==================================================
// ЗАДАНИЕ 3: Функции с возвратом значения
// ==================================================

console.log("ЗАДАНИЕ 3: Функции с возвратом значения");
console.log("------------------------------------------");

// 1. Создайте функцию add(a, b), которая возвращает сумму двух чисел
// 2. Создайте функцию isEven(num), которая возвращает true, если число чётное, иначе false
// 3. Создайте функцию getGrade(score), которая возвращает оценку:
//    - 90-100: "Отлично"
//    - 70-89: "Хорошо"
//    - 50-69: "Удовлетворительно"
//    - < 50: "Неудовлетворительно"
// 4. Вызовите функции и выведите результаты

// Ваш код:
// Пример:
// function square(num) {
//     return num * num;
// }
// let result = square(5);
// console.log(result);  // 25




console.log("");
console.log("================================");
console.log("");

// ==================================================
// ЗАДАНИЕ 4: Функции с несколькими параметрами
// ==================================================

console.log("ЗАДАНИЕ 4: Функции с несколькими параметрами");
console.log("------------------------------------------------");

// 1. Создайте функцию calculateRectangleArea(width, height), которая возвращает площадь прямоугольника
// 2. Создайте функцию getFullName(firstName, lastName, middleName), которая возвращает полное имя
//    (учтите, что отчество может быть не указано)
// 3. Вызовите функции с разными аргументами

// Ваш код:




console.log("");
console.log("================================");
console.log("");

// ==================================================
// ЗАДАНИЕ 5: Стрелочные функции
// ==================================================

console.log("ЗАДАНИЕ 5: Стрелочные функции");
console.log("-------------------------------");

// 1. Перепишите функцию add(a, b) из Задания 3 как стрелочную функцию
// 2. Создайте стрелочную функцию isAdult(age), которая возвращает true, если возраст >= 18
// 3. Создайте стрелочную функцию getGreeting(time), которая возвращает:
//    - "Доброе утро!" если время < 12
//    - "Добрый день!" если время < 18
//    - "Добрый вечер!" иначе
// 4. Вызовите функции и выведите результаты

// Ваш код:
// Пример:
// let square = x => x * x;
// console.log(square(5));  // 25




console.log("");
console.log("================================");
console.log("");

// ==================================================
// ЗАДАНИЕ 6: Практическая задача — калькулятор
// ==================================================

console.log("ЗАДАНИЕ 6: Калькулятор");
console.log("-----------------------");

// Создайте набор функций для калькулятора:
// 1. add(a, b) — сложение
// 2. subtract(a, b) — вычитание
// 3. multiply(a, b) — умножение
// 4. divide(a, b) — деление (проверьте деление на 0)
// 5. Протестируйте все функции

// Ваш код:




console.log("");
console.log("================================");
console.log("");

// ==================================================
// ЗАДАНИЕ 7: Функции высшего порядка
// ==================================================

console.log("ЗАДАНИЕ 7: Функции высшего порядка");
console.log("-------------------------------------");

// Функция высшего порядка — это функция, которая принимает другую функцию как параметр.

// 1. Создайте функцию applyOperation(a, b, operation), которая принимает два числа и функцию
//    и применяет эту функцию к числам
// 2. Создайте функции для сложения, вычитания, умножения
// 3. Вызовите applyOperation с разными операциями

// Пример:
// function add(x, y) { return x + y; }
// function applyOperation(a, b, operation) {
//     return operation(a, b);
// }
// let result = applyOperation(5, 3, add);  // 8

// Ваш код:




console.log("");
console.log("================================");
console.log("");

// ==================================================
// БОНУС: Рекурсия
// ==================================================

console.log("БОНУС: Рекурсия");
console.log("----------------");

// Рекурсия — это когда функция вызывает саму себя.

// Задача: Создайте функцию factorial(n), которая вычисляет факториал числа рекурсивно.
// Факториал: n! = n * (n-1) * (n-2) * ... * 1
// Пример: 5! = 5 * 4 * 3 * 2 * 1 = 120

// Подсказка:
// function factorial(n) {
//     if (n === 0 || n === 1) {
//         return 1;
//     }
//     return n * factorial(n - 1);
// }

// Ваш код:




console.log("");
console.log("УРОК 6 ЗАВЕРШЁН");
